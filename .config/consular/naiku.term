cd = "cd /Users/aaron/Sites/naiku &&"
window do
  pane do
    pane "#{cd} bin/rails s"
    pane "#{cd} tail -f log/development.log"
    pane "#{cd} bundle exec rails console"
    pane "#{cd} bundle exec guard"
  end
  tab do
    run "#{cd} v."
  end
end
