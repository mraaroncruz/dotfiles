cd = "cd /Users/aaron/Sites/naiku &&"
window do
  pane do
    pane "#{cd} bin/rails s"
    pane "#{cd} tail -f log/development.log"
    pane "#{cd} bin/rails console"
    pane "#{cd} bin/guard"
  end
  tab do
    run "#{cd} v."
  end
end
